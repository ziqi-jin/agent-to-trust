{
  "name": "agent-credit-lab",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "packages/*",
    "apps/*",
    "simulator"
  ],
  "scripts": {
    "test": "npm run test --workspace @acl/api && npm run test --workspace @acl/scoring && npm run test --workspace @acl/core && npm run test --workspace @acl/simulator && npm run test --workspace @acl/adapters && npm run test --workspace sealit-sdk",
    "typecheck": "npm run typecheck --workspace @acl/core && npm run typecheck --workspace @acl/scoring && npm run typecheck --workspace @acl/simulator && npm run typecheck --workspace @acl/api && npm run typecheck --workspace @acl/adapters && npm run typecheck --workspace sealit-sdk",
    "test:scoring": "npm run test --workspace @acl/scoring",
    "test:simulator": "npm run test --workspace @acl/simulator",
    "dev:api": "npm run dev --workspace @acl/api"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
